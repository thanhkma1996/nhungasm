; ;DE 8
; ;1.TINH TONG CAC SO CHIA HET CHO 5 TRONG CHUOI SO
; ;2.TINH UCLN TREN CHEO CHINH CUA MA TRAN

	; AREA RESET, DATA, READONLY
	; EXPORT _VECTOR
; _VECTOR
	; DCD 0x20001000
	; DCD RESET_HANDLER
	
	; AREA MYDATA, DATA, READONLY
; CHUOISO 
	; DCD 1,2,3,4,5,6,7,10
; MATRAN
	; DCD 4,0,0,0
	; DCD 0,4,0,0
	; DCD 0,0,6,0
	; DCD 0,0,0,8
	
	; AREA MYCODE, CODE, READONLY

; ;CHIA HET CHO 2
; TONGCHIA_5 PROC
	; LDR R4,[R0], #4
; Div5
	; MOV R5, #5
	; UDIV R6, R4, R5
	; MUL R7, R6, R5
	; SUB R8, R4, R7
	; CMP R8, #0
	; BEQ XuLy
	; B Exit
; XuLy
	; ADD R1, R1, R4
	; B Exit
; Exit
	; ADD R3, R3, #1
	; CMP R3, #8
	; BLE TONGCHIA_5
	; BX LR
		; ENDP
		
; ;TIM UCLN
; UCLN PROC
	; LDR R4, [R0], #20
; TimUCLN
	; CMP R4, R2		;SO SANH VS UCLN BAN DAU
	; BGT _A			;XU LY R4 = R4 - R1
	; BLT _B			;XU LY R1 = R1 -R4
	; BEQ	Exit2
; _A
	; SUB R4, R4, R2	; R4 = R4-r2
	; B TimUCLN
; _B
	; SUB R2, R2, R4	; R2 = R2-r4
	; B TimUCLN
; Exit2
	; ADD R3, R3, #1
	; CMP R3, #4			;SO SANH VS SO HANG MA TRAN
	; BLE UCLN
	; BX LR
		; ENDP
	
; RESET_HANDLER
; MAIN PROC
	; LDR R0, =CHUOISO	;DIA CHI CHUOI SO
	; MOV R1, #0		;SUM 5
	; MOV R3, #1		;BIEN CHAY
	; BL TONGCHIA_5
	; LDR R0, =MATRAN	;DIA CHI MA TRAN
	; LDR R2, [R0]	;UCLN
	; MOV R3, #1
	; BL UCLN
	; NOP
		; ENDP
		; END		;HamChoiqh
	
